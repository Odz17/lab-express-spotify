<div class="container">
  {{#each artists}}
    <div class="artist">
      <img src="{{images.0.url}}" alt="{{name}}" />
      <h2>{{name}}</h2>

      <button>
        <a href="/albums/{{id}}">View albums</a>
      </button>

    </div>
  {{/each}}
</div>
